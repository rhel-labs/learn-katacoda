---
- hosts: web
  tasks:
    - name: "Update Hostnames"
      hostname:
        name: "{{ new_hostname }}"

- hosts: all
  tasks:
  - name: Update all packages
    yum:
      name: "*"
      state: latest
